Embedded resources used by the plugin live here.

Files named `Ice.Resources.*` are embedded by `IceSource.csproj` with their filename as the logical resource name. Keep those filenames stable unless every matching `AssetHandler` lookup is updated too.
